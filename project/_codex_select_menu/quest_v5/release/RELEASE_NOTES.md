# Utage menu V5

Refines the approved menu designs, preserves natural proportions in both selection states, reduces offset shadows, and adds matching Gallery and Options artwork. Replaces the separate Japanese locked-character label with Locked across four active copies.

Repairs the compact P2 Join prompt by sampling only its complete phrase, setting the matching geometry, and placing it inside the existing frame. START input/icon and the frame geometry remain unchanged. Six differing instruction-row copies are brought into agreement with the startup atlas, including the root battleQuest copy.

Installed scope: 25 archives, 42 resources; 1,788 other compressed resources preserved exactly. Fourteen main-menu atlas copies, nine character-selection heading copies, four locked labels, two menu layouts, seven compact prompt layouts, and six instruction-row copies. The 84 layout field edits are individually audited; all other bytes remain unchanged. Texture changes are restricted to declared cells.

The package contains these changed archives only, built from the current live V4 baseline. It is not a complete game or cumulative translation distribution. Japanese reference, demo, character dialogue, conquest message files, reward names, stars and quest title cells are untouched.

Offline validation and installed-file readback do not constitute runtime verification. Fully stop and boot the game again, then check all seven normal/selected menu entries; the Japan's Event character-selection heading; a locked character; and the red START/P2 Join prompt before and after joining Player 2. The emulator was open during installation, so previously loaded resources may remain cached until restart.

Rollback: BACKUP_PRE_V5.zip restores the exact pre-install archive bytes. Validation and install manifests record source and result hashes.
