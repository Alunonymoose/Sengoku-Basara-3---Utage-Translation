Sengoku BASARA 3 Utage English Patch - Cockpit KOs Repair V8

This package is cumulative over V7. Merge PS3_GAME into the extracted game root and overwrite.

V8 repairs ENG and JPN cockpit1P/cockpit2P routes. It restores or preserves Utage's clean label atlas, appends Samurai Heroes' official 128x64 KOs texture byte-for-byte as a separate resource, and redirects only the Kill nodes. Utage-only labels and all unrelated resources remain intact.

Offline ARC, route-coverage, resource, byte-scope, ZIP, and checksum checks pass. A cold-boot gameplay test is still required for both 1P and 2P.
